# PLAN.md - 아직 안 끝난 것만 있는 파일

여기 있는 모든 내용은 목적에 맞는 곳으로 옮겨진다.
"오늘 무엇을 했는지"는 `records/`, 규칙은 `CLAUDE.md` 등 주제에 맞는 위치로 옮겨진다.
코드로 기록되는 거라면 주석이나 테스트 케이스로 옮긴다.

`PLAN.md` 파일에 기록되는 건 "할 일", "정하지 않은 것" 두 가지고 그 이유에 대한 내용은 3줄을 넘기지 않는다.

---

## 일정 — 9월 말까지 (2026-08-30 기준)

```
백엔드 연결   로그인 · 저장/조회 · mocks 제거          4~6일
사진 3슬롯    리사이즈 · 업로드 · 표시 · 삭제           3~4일
원두 파생값    한 잔 원가 · 소진 예상일 · D+n · beanId   2일
대시보드      통계 · 최근 기록 · 지금 마실 원두          3일
레시피        입력 + 푸어 단계                        3~4일
추출 타이머    백그라운드 · 화면 잠금 · 새로고침 견디기    3~4일
커뮤니티      피드 · 좋아요 · 댓글 · 신고 · 차단        6~8일
설정         JSON 내보내기/불러오기 · 소셜 연결 관리     2~3일
초안 보관                                          1~2일
                                          합계  27~36일
```

---

## 할 일

##### 260902 — 원두 DB 연결 (내일)

내일은 아래 순서로 진행한다. 각 항목은 별도 승인·검증 단위로 나누고, 이번 순서에서는 DB
스키마를 변경하지 않는다.

1. **DB row ↔ 앱 타입 매핑**

- [ ] `libs/db/bean-mappers.ts`에 `snake_case` row를 `Bean`으로 바꾸는 순수 함수를 만든다.
- [ ] `null` 선택값은 `undefined`로 바꾸고, DB의 `user_id`·`created_at`은 화면 타입에 노출하지 않는다.
- [ ] 같은 파일의 매핑 테스트를 작성한다.
- ▸ 완료 조건: 알려진 DB row와 폼의 정규화된 값이 기대한 `Bean`·insert row로 변환된다.

2. **원두 조회 어댑터**

- [ ] `libs/db/beans.ts`에 `listBeans()`와 `getBeanById(id)`만 먼저 만든다.
- [ ] `libs/db/server.ts`의 요청별 클라이언트를 사용하고 RLS가 현재 사용자 소유권을 검사하게 둔다.
- [ ] 화면과 목업은 아직 바꾸지 않는다.
- ▸ 완료 조건: 타입이 지정된 Supabase query가 컴파일되고, 오류는 사용자에게 노출할 내부 상세 없이 의미 있는 오류로 바뀐다.

3. **원두 추가 어댑터와 Server Action**

- [ ] `libs/db/beans.ts`에 `insertBean(input, userId)`를 추가한다.
- [ ] `app/(main)/(private)/beans/actions.ts`의 `createBean(input: unknown)`에서 `getUser()`를 확인한다.
- [ ] 같은 `beanSchema`로 서버에서 `safeParse`하고 성공한 `parsed.data`만 저장한다.
- ▸ 완료 조건: 비로그인 요청은 저장하지 않고, 잘못된 입력은 필드 오류로 돌아오며, 정상 입력은 RLS를 통과한 내 원두로 저장된다.

4. **원두 등록 폼 연결**

- [ ] `components/beans/new-bean-form.tsx`의 `console.log`를 `createBean` 호출로 교체한다.
- [ ] 저장 중 상태, 서버 필드 오류, 일반 오류를 표시하고 실패 시 입력값을 유지한다.
- [ ] 성공 시 모달을 닫고 `/beans` 목록을 갱신한다.
- ▸ 완료 조건: 로그인한 사용자가 화면에서 원두 하나를 등록하고 저장 결과를 확인할 수 있다.

5. **원두 목록을 DB 조회로 전환**

- [ ] `app/(main)/(private)/beans/layout.tsx`에서 목업 대신 `listBeans()`를 호출한다.
- [ ] 원두가 없을 때 현재 빈 상태 문구를 유지하고, 보관 원두 자동 전환·자동 제안은 하지 않는다.
- ▸ 완료 조건: 방금 등록한 원두가 새로고침 후에도 목록에 보인다.

6. **원두 상세와 기록 조회를 DB로 전환**

- [ ] `libs/db/brews.ts`에 row ↔ `Brew` 변환과 현재 사용자 기록 조회를 추가한다.
- [ ] `app/(main)/(private)/beans/[id]/page.tsx`에서 `getBeanById()`와 `bean_id` 기준 기록 조회를 사용한다.
- [ ] 잔량·기록 수·평균 점수가 실제 DB 기록으로 계산되는지 확인한다.
- ▸ 완료 조건: 다른 사용자의 ID를 URL에 넣어도 원두가 조회되지 않고, 내 원두 상세에는 연결된 기록만 보인다.

7. **목업 제거와 전체 검증**

- [ ] 목록·상세·등록의 실제 동작을 확인한 뒤에만 `libs/mocks/beans.ts`, `libs/mocks/brews.ts`를 제거한다.
- [ ] `pnpm test`, `pnpm lint`, `pnpm exec tsc --noEmit`, 개발 서버를 종료할 수 있을 때 `pnpm build`를 실행한다.
- [ ] 로그인 사용자 기준 모바일·데스크톱 브라우저와 RLS 소유권을 확인한다.

### 인증 — 아직 남은 일

- [ ] OAuth callback의 운영 canonical origin과 허용 Host 범위를 정한다. 개발용 `localhost` redirect와 분리한다.

### 원두 화면 — 아직 남은 일

- [ ] **`archived` — 원두 상세의 "모두 사용했다" 토글 하나.** 켜고 끄는 것도 여기서. 자동 전환이나 제안은 하지 않는다.
- [ ] **"전체 N건 보기"의 목적지**를 원두 필터(`/brews?bean=…`)로 만들지, 라벨에서 숫자를 뺄지 정한다.
- [ ] g당 가격·한 잔 원가·소진 예상일의 표시 여부와 값 부족 시 화면 문구를 실제 DB 데이터로 확인하며 정한다.

### 백엔드 붙이기

폼은 다 도는데 `handleSubmit` 안이 `console.log` 다. `libs/mocks/*` 는 배열 상수라 넣을 데가 없다.

**입력 검증**

- [ ] 서버 액션에서 같은 `beanSchema.safeParse`를 다시 실행한다. 폼의 `zodResolver`가
      HTML 문자열을 정규화해도 서버 입력은 신뢰하지 않는다.
- [ ] 성공한 `parsed.data`만 저장하고, `Assert`가 여전히 `types/*.ts`를 지키는지 확인한다.

**데이터**

- [ ] `recipes`는 나중에 (명세 5장)

**사진** — 명세 3.2의 3슬롯. 아직 스키마에도 없다

크기는 정해졌다: **썸네일 400px · 확대본 2000px · 둘 다 WebP.** 200건에 약 175MB.
레이아웃에서 뽑은 계산 근거는 `records/2026-08-29.md`.

- [ ] **원본을 저장하지 않는다.** 넣으면 200건 × 3장 × 4MB = 2.4GB로 무료 1GB를 바로 넘는다
- [ ] 두 벌을 **클라이언트에서** 만들어 올린다 — 400px q75(~40KB) · 2000px q80(~250KB).
      Supabase 이미지 변환은 **Pro 전용**이라 URL로 크기를 부르는 방식을 못 쓴다
- [ ] **확대본을 1600px으로 줄이지 않는다** — 봉투에 인쇄된 글자가 사진보다 먼저 무너진다
- [ ] **함정: EXIF 회전.** `createImageBitmap(file, { imageOrientation: "from-image" })`.
      빠뜨리면 아이폰 세로 사진만 누워서 올라가는데, **발견 시점엔 원본을 이미 버린 뒤다**
- [ ] 확대본은 **눌러야 로드한다.** 미리 받아두면 무료 대역폭 5GB가 의미를 갖는다
- [ ] **데이터베이스에는 경로만 저장한다.** 도메인은 코드에서 붙인다.
      전체 주소(`https://xxx.supabase.co/...`)를 저장하면 저장소를 옮길 때 데이터를 전부 고쳐야 한다
- [ ] **저장소 조작은 `libs/storage.ts` 한 곳에만.** 올리기 · 지우기 · 주소 만들기.
      여기만 Supabase 를 알면 나중에 옮길 때 이 파일 하나만 바꾼다

확인 — 봉투 사진을 두 크기로 만들어 40KB · 250KB 근처인지, 2000px에서 **로스터리 이름이 읽히는지**

**연결**

- [ ] 기록 저장도 같이
- [ ] 저장 후 목록 갱신 — `revalidatePath` 인지 `router.refresh()` 인지
- [ ] 서버에서만 나는 에러를 폼에 표시 (`setError`)
- [ ] 저장 실패 시 입력값 보존 → 위 "초안 보관" 과 이어진다

**확인 수단**

- [ ] **서버 액션은 현재 Vitest 범위 밖이다.** 실제 로그인 브라우저와 RLS로 확인한다.

### 폼 남은 칸

A5~A9(RHF 연결 · 타입 · 토글 · 센서리 · 제출)는 전부 끝났다 — `records/2026-08-26.md` · `2026-08-27.md`.

- [ ] 사진 3슬롯 (위 백엔드와 같이)
- [ ] 원두 선택 카드

### 그 밖

- [ ] `records/2026-08-27.md` 의 **★ 표시 함정 10개를 코드 주석으로** 옮기기.
      그중 `as Route` 하나가 `form-dialog.tsx:58` 에 붙으면 규칙 예외 설명이 코드로 들어간다

---

## 미뤄둔 결정

| 무엇                                                                              | 언제 정하나                                                           |
| --------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| `dose` · `water` · `price` 의 상한                                                | 값 범위를 손볼 때 같이                                                |
| 기록 추가 시(집) 레시피 등록 및 기록 <-> 레시피 연계                              | 레시피 화면 개발 시                                                   |
| 기록 공유 토글 버튼 추가                                                          | 커뮤니티 개발 시                                                      |
| 기록 검색 필터링 추가                                                             | 백엔드 구축 후 기록 API 만들 시                                       |
| `import type` 스타일을 프로젝트 전체로 통일할지                                   | 나중에 한 번에                                                        |
| 토글 마크업 — `<button aria-pressed>` vs `<fieldset>`+라디오                      | 접근성 손볼 때. 라디오가 정석이나 폼 바깥이라 어정쩡                  |
| 모달 2단 비율 `1fr 1.2fr` — 시안은 `1fr 1fr` + `gap 24px`, 세로선 없음            | 눈에 거슬리면                                                         |
| "남은 용량(추정)" 은 명세 4장 파생값 목록에 없다 (6.1 B-1 확장 후보에서 온 것)    | 소진 예상일을 만들 때 같이                                            |
| 원두 목록에 필터 · 정렬을 둘지 (명세 3.2 "보유 원두 우선 정렬")                   | 원두가 늘어나면                                                       |
| header · panes · list 껍데기를 공통화할지 (`/brews` 와 `/beans` 가 사실상 복사본) | **원두 등록까지 끝난 뒤.** 지금 빼면 축이 덜 확정된 채 굳는다         |
| 저장 성공을 토스트로 알릴지 (지금은 목록 갱신만)                                  | 저장이 실제로 붙은 뒤                                                 |
| 탭 제목 템플릿 (`title: { template: "%s · koi" }`) 을 루트에 둘지                 | 아무 때나. 지금은 상세만 자기 제목을 가진다                           |
| 앱 셸에 `h-full` 대신 `h-dvh` 가 필요한지 (iOS 주소창)                            | 사파리는 멀쩡. **크롬(아이패드)에서만 전체 스크롤.** 크롬까지 챙길 때 |
